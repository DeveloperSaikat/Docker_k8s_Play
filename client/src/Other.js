import React from "react";
import { Link } from "react-router-dom";

const Other = () => {
    return (
        <div>  
            Wrong URL, try the below link!!
            <Link to="/"> Go back</Link>
        </div>
    );
};

export default Other;

